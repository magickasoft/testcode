import * as React from 'react';
import { Search } from 'components/Search';

import styles from './styles.module.css';

const Actions = React.memo(() => <div className={styles.actions}>{/* <Search /> */}</div>);

export { Actions };
