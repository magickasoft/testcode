import { createEntitySelectors } from 'utils/entity';
import { TAX_RECONCILLIATION_DELETION_ID } from './constants';

export const taxReconcilliationDeletionSelector = createEntitySelectors(TAX_RECONCILLIATION_DELETION_ID);
