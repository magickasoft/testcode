import { createEntitySelectors } from 'utils/entity';
import { TAX_RECONCILLIATION_EDIT_ID } from './constants';

export const taxReconcilliationFormSelector = createEntitySelectors(TAX_RECONCILLIATION_EDIT_ID);
