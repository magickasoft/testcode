import { createListSelectors } from 'utils/list';
import { TAX_RECONCILLIATION_ANALYTICS_ID } from './constants';

export const taxReconcilliationAnalyticsSelectors = createListSelectors(TAX_RECONCILLIATION_ANALYTICS_ID);
