import { createEntitySelectors } from 'utils/entity';
import { INVOICE_SUBMITTAL_DELETION_ID } from './constants';

export const invoiceSubmittalDeletionSelector = createEntitySelectors(INVOICE_SUBMITTAL_DELETION_ID);
