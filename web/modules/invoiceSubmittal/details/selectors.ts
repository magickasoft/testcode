import { createListSelectors } from 'utils/list';
import { INVOICE_SUBMITTAL_DETAILS_ID } from './constants';

export const invoiceSubmittalDetailsSelectors = createListSelectors(INVOICE_SUBMITTAL_DETAILS_ID);
