import { createEntitySelectors } from 'utils/entity';
import { INVOICE_SUBMITTAL_EDIT_ID } from './constants';

export const invoiceSubmittalFormSelector = createEntitySelectors(INVOICE_SUBMITTAL_EDIT_ID);
