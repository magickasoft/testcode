import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginHorizontal: 12,
    marginTop: 12,
    alignItems: 'center',
    justifyContent: 'space-between'
  }
});
