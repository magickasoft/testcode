import { StyleSheet } from 'react-native';
import { dimensions } from '../../../styles';

const styles = StyleSheet.create({
  container: {
    paddingBottom: dimensions.indent * 3,
  },
  text: {
    marginBottom: dimensions.indent * 2,
  },
});

export default styles;
